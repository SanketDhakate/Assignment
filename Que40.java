/* Write a program to print the multiplication 
   table of a number using a for loop. */
class Que40{
   public static void main(String[] args) {
      int num = 3;
      for(int i = 1; i <= 10; i++){
         System.out.println(num + " * " + i + " = " + (num * i));
      }
   }
}